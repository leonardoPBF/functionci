name: Deploy Azure Function with RBAC

on:
  push:
    branches:
      - main

env:
  AZURE_FUNCTIONAPP_NAME: 'silabosUsmp'
  AZURE_FUNCTIONAPP_PACKAGE_PATH: './backend'
  NODE_VERSION: '20.x'  # ✅ Versión LTS estable

jobs:
  build-and-deploy:
    runs-on: windows-latest
    environment: production
    
    steps:
      # 1️⃣ Descargar el código
      - name: Checkout repository
        uses: actions/checkout@v4
       
      # 2️⃣ Configurar Node.js
      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: '${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}/package-lock.json'
       
      # 3️⃣ Instalar dependencias
      - name: Install dependencies
        working-directory: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
        run: npm ci
       
      # 4️⃣ Ejecutar tests (opcional)
      - name: Run tests
        working-directory: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
        run: npm test
        continue-on-error: true
       
      # 5️⃣ Login a Azure usando Service Principal
      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
       
      # 6️⃣ Configurar runtime de Azure Function explícitamente
      - name: Set Function App Runtime
        run: |
          az functionapp config appsettings set `
            --name ${{ env.AZURE_FUNCTIONAPP_NAME }} `
            --resource-group silabosUsmp_group `
            --settings "FUNCTIONS_NODE_VERSION=~20" `
                      "WEBSITE_NODE_DEFAULT_VERSION=~20" `
                      "FUNCTIONS_EXTENSION_VERSION=~4"
        shell: powershell
       
      # 7️⃣ Desplegar usando Azure Functions Action con RBAC
      - name: Deploy to Azure Function
        uses: azure/functions-action@v1
        with:
          app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
          package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          slot-name: 'production'
          respect-funcignore: true
       
      # 8️⃣ Verificar el estado del deployment
      - name: Verify deployment
        run: |
          Write-Host "Checking function app status..."
          az functionapp show --name ${{ env.AZURE_FUNCTIONAPP_NAME }} --resource-group silabosUsmp_group --query "state"
        shell: powershell
       
      # 9️⃣ Logout de Azure (buena práctica de seguridad)
      - name: Logout from Azure
        run: az logout
        if: always()